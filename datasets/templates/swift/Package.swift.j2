// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "{{ project_pascal }}",
    platforms: [
        .macOS(.v13)
    ],
    products: [
        .executable(name: "{{ project_pascal }}", targets: ["App"])
    ],
    targets: [
        .executableTarget(
            name: "App",
            path: "Sources/App"
        ),
        .testTarget(
            name: "AppTests",
            dependencies: ["App"],
            path: "Tests/AppTests"
        )
    ]
)
