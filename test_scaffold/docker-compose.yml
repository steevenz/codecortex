# Myawesomeproject — Docker Compose
# Generated by PyScaffold

services:
  app:
    build: .
    container_name: myawesomeproject
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app/src

  # Uncomment to add a database service
  # db:
  #   image: postgres:16-alpine
  #   container_name: myawesomeproject-db
  #   environment:
  #     POSTGRES_DB: myawesomeproject
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

# volumes:
#   pgdata:
